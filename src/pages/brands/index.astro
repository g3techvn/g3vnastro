---
import Layout from '../../layouts/Layout.astro';
import { supabase } from '../../lib/supabase';

let brands: Array<{
  id: number;
  title: string;
  slug: string;
  image_url?: string;
  image_square_url?: string;
}> = [];

try {
  // Lấy dữ liệu trực tiếp từ Supabase
  const { data, error } = await supabase
    .from('brands')
    .select('*')
    .order('title', { ascending: true });

  if (error) {
    console.error('Error fetching brands:', error);
  } else {
    brands = data || [];
  }
} catch (error) {
  console.error('Error fetching brands:', error);
}

const title = 'Thương hiệu | G3 - Công Thái Học';
const description = 'Khám phá các thương hiệu nội thất công thái học tại G3.vn. Nội thất công thái học, bảo hành 12 tháng, giao hàng toàn quốc.';
---

<Layout title={title} description={description}>
  <!-- Mobile View -->
  <div class="md:hidden">
    <div class="px-4 py-4">
      {brands.length > 0 ? (
        <div class="grid grid-cols-2 gap-4">
          {brands.map((brand: any) => (
            <a href={`/brands/${brand.slug}`} class="block">
              <div class="rounded-lg border border-gray-200 bg-white p-4 shadow-sm">
                <div class="aspect-square w-full overflow-hidden rounded-lg bg-gray-100">
                  {brand.image_square_url || brand.image_url ? (
                    <img
                      src={brand.image_square_url || brand.image_url}
                      alt={brand.title}
                      class="h-full w-full object-contain"
                    />
                  ) : (
                    <div class="flex h-full items-center justify-center">
                      <span class="text-2xl font-bold text-gray-400">
                        {brand.title.charAt(0)}
                      </span>
                    </div>
                  )}
                </div>
                <div class="mt-4">
                  <h3 class="text-lg font-semibold text-gray-900">
                    {brand.title}
                  </h3>
                </div>
              </div>
            </a>
          ))}
        </div>
      ) : (
        <div class="rounded-lg border border-gray-200 bg-white p-4 text-center shadow-sm">
          <p class="text-base text-gray-600">Không tìm thấy thương hiệu nào.</p>
        </div>
      )}
    </div>
  </div>

  <!-- Desktop View -->
  <div class="hidden md:block container mx-auto px-2 py-4">
    {brands.length > 0 ? (
      <div class="grid grid-cols-3 gap-4 lg:grid-cols-5">
        {brands.map((brand: any) => (
          <a href={`/brands/${brand.slug}`} class="block">
            <div class="rounded-lg border border-gray-200 bg-white p-4 shadow-sm hover:shadow-md transition-shadow">
              <div class="aspect-square w-full overflow-hidden rounded-lg bg-gray-100">
                {brand.image_square_url || brand.image_url ? (
                  <img
                    src={brand.image_square_url || brand.image_url}
                    alt={brand.title}
                    class="h-full w-full object-contain"
                  />
                ) : (
                  <div class="flex h-full items-center justify-center">
                    <span class="text-2xl font-bold text-gray-400">
                      {brand.title.charAt(0)}
                    </span>
                  </div>
                )}
              </div>
              <div class="mt-4">
                <h3 class="text-lg font-semibold text-gray-900">
                  {brand.title}
                </h3>
              </div>
            </div>
          </a>
        ))}
      </div>
    ) : (
      <div class="rounded-lg border border-gray-200 bg-white p-4 text-center shadow-sm">
        <p class="text-base text-gray-600">Không tìm thấy thương hiệu nào.</p>
      </div>
    )}
  </div>
</Layout>

<style>
  .aspect-square {
    aspect-ratio: 1 / 1;
  }
</style> 